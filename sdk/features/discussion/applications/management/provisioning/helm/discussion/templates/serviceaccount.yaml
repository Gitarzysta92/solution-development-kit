{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "discussion.fullname" .) }}
  labels:
    {{- include "discussion.labels" . | nindent 4 }}
{{- end }}

