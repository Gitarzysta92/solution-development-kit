{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "discussion-materializer.fullname" .) }}
  labels:
    {{- include "discussion-materializer.labels" . | nindent 4 }}
{{- end }}

